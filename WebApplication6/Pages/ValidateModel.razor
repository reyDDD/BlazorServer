@page "/valid"
<h3>ValidateModel</h3>

<EditForm Model="@model" OnValidSubmit="SendDataFromForm">
    <DataAnnotationsValidator />
    <ValidationSummary />
    <InputNumber @bind-Value="model.Id" />
    <InputText @bind-Value="model.Name" />
    <button type="submit">Отправить</button>
</EditForm>

@code {
    Model1411 model = new Model1411();
    private void SendDataFromForm()
    {
        //work with data here...
    }
}
